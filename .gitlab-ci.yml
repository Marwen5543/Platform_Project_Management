# This pipeline has a dedicated 'test' stage for the user-service.
# This version is corrected to handle job dependencies and rules properly.

stages:
  - build-and-test
  - push

# Reusable template for pushing to Docker Hub
.build_and_push_template:
  stage: push
  image: docker:20.10.16
  services:
    - docker:20.10.16-dind
  before_script:
    - echo "$DOCKERHUB_TOKEN" | docker login -u "$DOCKERHUB_USERNAME" --password-stdin
  script:
    - echo "Building Docker image for $MICROSERVICE_NAME..."
    - docker build -t "$DOCKERHUB_USERNAME/$MICROSERVICE_NAME:$CI_COMMIT_SHORT_SHA" -f "$MICROSERVICE_PATH/Dockerfile" .
    - echo "Pushing new image version to Docker Hub..."
    - docker push "$DOCKERHUB_USERNAME/$MICROSERVICE_NAME:$CI_COMMIT_SHORT_SHA"

# ===================================================================
# JOBS FOR: user-management-service (Corrected Structure)
# ===================================================================
# -- Job 1: Build the code AND run unit tests in a single, visible step --
unit-tests:user-service:
  stage: build-and-test # We use a single stage for build and test
  image: maven:3.8.5-openjdk-17
  script:
    - echo "Building and testing user-management-service..."
    - cd user-management-service
    - mvn clean package # This command compiles AND runs tests
  artifacts:
    # Save the created .jar file for the next stage
    paths:
      - user-management-service/target/*.jar
    # Also save the test reports for viewing in the GitLab UI
    reports:
      junit:
        - user-management-service/target/surefire-reports/TEST-*.xml
  rules:
    # This entire job runs only if files in this service's folder change
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes:
        - user-management-service/**/*

# -- Job 2: Push the Docker image --
push:user-service:
  extends: .build_and_push_template
  variables:
    MICROSERVICE_NAME: user-management-service
    MICROSERVICE_PATH: user-management-service
  needs:
    # It now depends on the combined test job
    - job: unit-tests:user-service
      artifacts: true # This tells GitLab to download the artifacts (the .jar file)
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes:
        - user-management-service/**/*


# ===================================================================
# JOBS FOR: document-management-service (Unchanged, but still works)
# ===================================================================
build:document-service:
  stage: build-and-test # It will run in the same first stage
  image: maven:3.8.5-openjdk-17
  script:
    - cd Document-management-service
    - mvn clean package
  artifacts:
    paths:
      - Document-management-service/target/*.jar
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes:
        - Document-management-service/**/*

push:document-service:
  extends: .build_and_push_template
  variables:
    MICROSERVICE_NAME: document-management-service
    MICROSERVICE_PATH: Document-management-service
  needs: [build:document-service]
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes:
        - Document-management-service/**/*

# ===================================================================
# JOBS FOR: leave-management-service (Unchanged, but still works)
# ===================================================================
build:leave-service:
  stage: build-and-test # It will run in the same first stage
  image: maven:3.8.5-openjdk-17
  script:
    - cd leave-management-service
    - mvn clean package
  artifacts:
    paths:
      - leave-management-service/target/*.jar
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes:
        - leave-management-service/**/*

push:leave-service:
  extends: .build_and_push_template
  variables:
    MICROSERVICE_NAME: leave-management-service
    MICROSERVICE_PATH: leave-management-service
  needs: [build:leave-service]
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes:
        - leave-management-service/**/*
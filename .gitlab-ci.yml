# ===================================================================
# FINAL, SIMPLE, AND SYNTACTICALLY CORRECT VERSION
# ===================================================================
default:
  tags:
    - docker
    
stages:
  - build-and-deploy-to-nexus
  - code-analysis
  - push-to-dockerhub

# ===================================================================
# JOBS FOR: user-management-service
# ===================================================================
build-and-deploy:user-service:
  stage: build-and-deploy-to-nexus
  image: maven:3.8.5-openjdk-17
  script:
    - echo "Building, testing, and deploying user-management-service to Nexus..."
    - cd user-management-service
    - mvn clean deploy -s $SETTINGS_XML
  artifacts:
    paths:
      - user-management-service/target/*.jar
    reports:
      junit:
        - user-management-service/target/surefire-reports/TEST-*.xml
    expire_in: 1 hour
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes:
        - "user-management-service/**/*"

sonar-analysis:user-service:
  stage: code-analysis
  image: maven:3.8.5-openjdk-17
  script:
    - echo "Running SonarQube analysis for user-management-service..."
    - cd user-management-service
    - >-
      mvn verify sonar:sonar -s $SETTINGS_XML
      -Dsonar.projectKey=com.tunisys:user-management-service
      -Dsonar.host.url=$SONAR_HOST_URL
      -Dsonar.login=$SONAR_TOKEN
      -Dsonar.qualitygate.wait=true
  needs:
    - job: build-and-deploy:user-service
      artifacts: true
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes:
        - "user-management-service/**/*"

push:user-service:
  stage: push-to-dockerhub
  image:
    name: gcr.io/kaniko-project/executor:v1.9.0-debug
    entrypoint: [""]
  script:
    - echo "Building Docker image for user-management-service..."
    - mkdir -p /kaniko/.docker
    - echo "{\"auths\":{\"https://index.docker.io/v1/\":{\"auth\":\"$(echo -n ${DOCKERHUB_USERNAME}:${DOCKERHUB_TOKEN} | base64 | tr -d '\n')\"}}}" > /kaniko/.docker/config.json
    - /kaniko/executor
      --context $CI_PROJECT_DIR/user-management-service
      --dockerfile $CI_PROJECT_DIR/user-management-service/Dockerfile
      --destination $DOCKERHUB_USERNAME/user-management-service:$CI_COMMIT_SHORT_SHA
      --destination $DOCKERHUB_USERNAME/user-management-service:latest
  needs:
    - job: build-and-deploy:user-service
      artifacts: true
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes:
        - "user-management-service/**/*"

# ===================================================================
# JOBS FOR: document-management-service
# ===================================================================
build-and-deploy:document-service:
  stage: build-and-deploy-to-nexus
  image: maven:3.8.5-openjdk-17
  script:
    - echo "Building, testing, and deploying document-management-service to Nexus..."
    - cd Document-management-service
    - mvn clean deploy -s $SETTINGS_XML
  artifacts:
    paths:
      - Document-management-service/target/*.jar
    reports:
      junit:
        - Document-management-service/target/surefire-reports/TEST-*.xml
    expire_in: 1 hour
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes:
        - "Document-management-service/**/*"

sonar-analysis:document-service:
  stage: code-analysis
  image: maven:3.8.5-openjdk-17
  script:
    - echo "Running SonarQube analysis for document-management-service..."
    - cd Document-management-service
    - >-
      mvn verify sonar:sonar -s $SETTINGS_XML
      -Dsonar.projectKey=com.tunisys:document-management-service
      -Dsonar.host.url=$SONAR_HOST_URL
      -Dsonar.login=$SONAR_TOKEN
      -Dsonar.qualitygate.wait=true
  needs:
    - job: build-and-deploy:document-service
      artifacts: true
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes:
        - "Document-management-service/**/*"

push:document-service:
  stage: push-to-dockerhub
  image:
    name: gcr.io/kaniko-project/executor:v1.9.0-debug
    entrypoint: [""]
  script:
    - echo "Building Docker image for document-management-service..."
    - mkdir -p /kaniko/.docker
    - echo "{\"auths\":{\"https://index.docker.io/v1/\":{\"auth\":\"$(echo -n ${DOCKERHUB_USERNAME}:${DOCKERHUB_TOKEN} | base64 | tr -d '\n')\"}}}" > /kaniko/.docker/config.json
    - /kaniko/executor
      --context $CI_PROJECT_DIR/Document-management-service
      --dockerfile $CI_PROJECT_DIR/Document-management-service/Dockerfile
      --destination $DOCKERHUB_USERNAME/document-management-service:$CI_COMMIT_SHORT_SHA
      --destination $DOCKERHUB_USERNAME/document-management-service:latest
  needs:
    - job: build-and-deploy:document-service
      artifacts: true
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes:
        - "Document-management-service/**/*"

# ===================================================================
# JOBS FOR: leave-management-service
# ===================================================================
build-and-deploy:leave-service:
  stage: build-and-deploy-to-nexus
  image: maven:3.8.5-openjdk-17
  script:
    - echo "Building, testing, and deploying leave-management-service to Nexus..."
    - cd leave-management-service
    - mvn clean deploy -s $SETTINGS_XML
  artifacts:
    paths:
      - leave-management-service/target/*.jar
    reports:
      junit:
        - leave-management-service/target/surefire-reports/TEST-*.xml
    expire_in: 1 hour
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes:
        - "leave-management-service/**/*"

sonar-analysis:leave-service:
  stage: code-analysis
  image: maven:3.8.5-openjdk-17
  script:
    - echo "Running SonarQube analysis for leave-management-service..."
    - cd leave-management-service
    - >-
      mvn verify sonar:sonar -s $SETTINGS_XML
      -Dsonar.projectKey=com.tunisys:leave-management-service
      -Dsonar.host.url=$SONAR_HOST_URL
      -Dsonar.login=$SONAR_TOKEN
      -Dsonar.qualitygate.wait=true
  needs:
    - job: build-and-deploy:leave-service
      artifacts: true
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes:
        - "leave-management-service/**/*"

push:leave-service:
  stage: push-to-dockerhub
  image:
    name: gcr.io/kaniko-project/executor:v1.9.0-debug
    entrypoint: [""]
  script:
    - echo "Building Docker image for leave-management-service..."
    - mkdir -p /kaniko/.docker
    - echo "{\"auths\":{\"https://index.docker.io/v1/\":{\"auth\":\"$(echo -n ${DOCKERHUB_USERNAME}:${DOCKERHUB_TOKEN} | base64 | tr -d '\n')\"}}}" > /kaniko/.docker/config.json
    - /kaniko/executor
      --context $CI_PROJECT_DIR/leave-management-service
      --dockerfile $CI_PROJECT_DIR/leave-management-service/Dockerfile
      --destination $DOCKERHUB_USERNAME/leave-management-service:$CI_COMMIT_SHORT_SHA
      --destination $DOCKERHUB_USERNAME/leave-management-service:latest
  needs:
    - job: build-and-deploy:leave-service
      artifacts: true
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes:
        - "leave-management-service/**/*"
# ===================================================================
# .gitlab-ci.yml - FINAL VERSION WITH SONARQUBE
# ===================================================================

stages:
  - build-and-test
  - code-analysis  # New stage for SonarQube
  - push

# Reusable template for SonarQube analysis
.sonar_scanner_template:
  stage: code-analysis
  image: maven:3.8.5-openjdk-17
  script:
    - echo "Running SonarQube analysis for $MICROSERVICE_NAME..."
    - cd $MICROSERVICE_PATH
    # The 'verify' phase runs tests, and 'sonar:sonar' sends the results
    - >-
      mvn verify sonar:sonar
      -Dsonar.projectKey=$SONAR_PROJECT_KEY
      -Dsonar.host.url=$SONAR_HOST_URL
      -Dsonar.login=$SONAR_TOKEN
      -Dsonar.qualitygate.wait=true
  allow_failure: false # Fail the pipeline if the Quality Gate fails

# Reusable template for pushing to Docker Hub
.build_and_push_template:
  stage: push
  image: docker:20.10.16
  services:
    - docker:20.10.16-dind
  before_script:
    - echo "$DOCKERHUB_TOKEN" | docker login -u "$DOCKERHUB_USERNAME" --password-stdin
  script:
    - docker build -t "$DOCKERHUB_USERNAME/$MICROSERVICE_NAME:$CI_COMMIT_SHORT_SHA" -f "$MICROSERVICE_PATH/Dockerfile" .
    - docker push "$DOCKERHUB_USERNAME/$MICROSERVICE_NAME:$CI_COMMIT_SHORT_SHA"

# ===================================================================
# JOBS FOR: user-management-service
# ===================================================================
unit-tests:user-service:
  stage: build-and-test
  image: maven:3.8.5-openjdk-17
  script:
    - cd user-management-service
    - mvn clean package
  artifacts:
    paths:
      - user-management-service/target/
    reports:
      junit:
        - user-management-service/target/surefire-reports/TEST-*.xml
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes:
        - user-management-service/**/*

sonar-analysis:user-service:
  extends: .sonar_scanner_template
  variables:
    MICROSERVICE_NAME: user-management-service
    MICROSERVICE_PATH: user-management-service
    SONAR_PROJECT_KEY: com.tunisys:user-management-service # The key you created in the SonarQube UI
  needs:
    - job: unit-tests:user-service
      artifacts: true
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes:
        - user-management-service/**/*

push:user-service:
  extends: .build_and_push_template
  variables:
    MICROSERVICE_NAME: user-management-service
    MICROSERVICE_PATH: user-management-service
  needs:
    - job: sonar-analysis:user-service # Depends on SonarQube analysis passing
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes:
        - user-management-service/**/*

# ===================================================================
# JOBS FOR: document-management-service
# ===================================================================
build:document-service:
  stage: build-and-test
  image: maven:3.8.5-openjdk-17
  script:
    - cd Document-management-service
    - mvn clean package
  artifacts:
    paths:
      - Document-management-service/target/
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes:
        - Document-management-service/**/*

sonar-analysis:document-service:
  extends: .sonar_scanner_template
  variables:
    MICROSERVICE_NAME: document-management-service
    MICROSERVICE_PATH: Document-management-service
    SONAR_PROJECT_KEY: com.tunisys:document-management-service # Create this project key in SonarQube
  needs:
    - job: build:document-service
      artifacts: true
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes:
        - Document-management-service/**/*

push:document-service:
  extends: .build_and_push_template
  variables:
    MICROSERVICE_NAME: document-management-service
    MICROSERVICE_PATH: Document-management-service
  needs:
    - job: sonar-analysis:document-service
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes:
        - Document-management-service/**/*

# ===================================================================
# JOBS FOR: leave-management-service
# ===================================================================
build:leave-service:
  stage: build-and-test
  image: maven:3.8.5-openjdk-17
  script:
    - cd leave-management-service
    - mvn clean package
  artifacts:
    paths:
      - leave-management-service/target/
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes:
        - leave-management-service/**/*

sonar-analysis:leave-service:
  extends: .sonar_scanner_template
  variables:
    MICROSERVICE_NAME: leave-management-service
    MICROSERVICE_PATH: leave-management-service
    SONAR_PROJECT_KEY: com.tunisys:leave-management-service # Create this project key in SonarQube
  needs:
    - job: build:leave-service
      artifacts: true
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes:
        - leave-management-service/**/*

push:leave-service:
  extends: .build_and_push_template
  variables:
    MICROSERVICE_NAME: leave-management-service
    MICROSERVICE_PATH: leave-management-service
  needs:
    - job: sonar-analysis:leave-service
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes:
        - leave-management-service/**/*